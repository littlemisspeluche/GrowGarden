<%= render 'components/banner' %>
<div class="container">
  <div class="row">
    <% @categories.each do |category| %>
    <%= render 'components/card', category: category %>
    <% end %>
  </div>
</div>

